// client/src/screens/securityTicker.ts

/**
 * The dim security marquee text shared by the loading screen and the waiting
 * room, so the two stay in sync. The trailing separator is intentional so the
 * string tiles cleanly when repeated end to end.
 */
export const SECURITY_TICKER_TEXT =
  "END-TO-END ENCRYPTED · ZERO KNOWLEDGE RELAY · KEYS STAY ON DEVICE · NO ACCOUNTS · NO METADATA · ";
